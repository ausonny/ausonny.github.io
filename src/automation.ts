// eslint-disable-next-line no-unused-vars
class Automation {
  item: number;

  value: number;
  constructor (item: number, value: number) {
    this.item = item;
    this.value = value;
  }
}
